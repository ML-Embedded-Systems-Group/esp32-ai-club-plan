# H1: ML + embedded, week 1

Member: ____________
Time budget: 8-10 hours across the week.

## Phase 1: Reading (about 2-3 hours)

Read this article: Kipply, "Transformer Inference Arithmetic".

https://kipp.ly/transformer-inference-arithmetic/

You need the memory bandwidth section: bytes per token = 2 times parameters (fp16), and tokens per second = bandwidth divided by bytes per token.

For the article, write in your log:
- what it is, in one line
- 3 things you learned
- 1 question you still have

Write in your own words. Do not copy-paste.

## Phase 2: Maths (about 2 hours)

Problem 1: compute bytes per token for the model sizes the article lists, in fp16. Show every step.

Verify: your values must match the article's table.

Problem 2: take the article's example GPU bandwidth. Compute tokens per second. Show every step.

Verify: your value must match the article's table.

Problem 3: the club model is 14.9 MB at 4 bits, so 0.5 bytes per parameter. Compute bytes per token.

Problem 4: the chip shows 9.88 tokens per second. Compute the bandwidth the chip must sustain. Show the unit conversions.

This is the meeting check. The lead asks you 2 questions from this phase.

## Phase 3: Coding, TOKS_REPRO.py (about 2-3 hours)

Write a script, under 40 lines, stdlib only:

1. Implement: bytes per token = parameters times bytes per parameter. Tokens per second = bandwidth divided by bytes per token.
2. Reproduce the article's table for the model sizes and bandwidths the article lists. Print the table.
3. Verify: your table matches the article's. Print pass or fail, with the biggest difference.
4. Club case: 14.9 MB at 4 bits. Print bytes per token and the bandwidth for 9.88 tokens per second.
5. Run the club case at 4 bit, 8 bit, and fp16. Print the three bandwidths.

Save it as TOKS_REPRO.py. Write the script yourself. Do not paste from any AI tool.

## Phase 4: Doc generation, TOKS.md (about 2 hours)

Write TOKS.md by hand, in your own words, at least 350 words. No drawings.

Required sections:

- The article in your own words: what it computes and why.
- The maths from phase 2, every step.
- The output of TOKS_REPRO.py, pasted.
- The comparison: your table vs the article's, and the three club bandwidths.
- A short section: what you did not understand this week, and your question for the meetup.

Write every sentence yourself. Do not paste from any AI tool. The meetup questions will show the difference.

Push TOKS.md and TOKS_REPRO.py to this repo.

## Meetup demo

- 5 minutes: run the script live and explain the bandwidth math.
- The lead asks you 2 questions about the maths.

## Evidence checklist (this is what closes your issue)

- [ ] Reading log, one entry per source
- [ ] TOKS_REPRO.py in this repo, with its output
- [ ] TOKS.md in this repo, 350+ words, own words
- [ ] Pair sign-off (R5 checks your log and your doc)
- [ ] Meetup demo given

## Pair

Your pair is R5. Check each other's logs and docs before the meetup.

## Resources

- Article: https://kipp.ly/transformer-inference-arithmetic/
