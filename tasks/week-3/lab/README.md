# Week 3 lab

Starter files for job 1.

1. Copy `student_forward.py` and `check.py` to `submissions/week-3/<member>/`.
2. Keep `weights.npz`, `idx.npy`, and `golden_logits.npy` here, or copy them too.
3. Fill `forward` in `student_forward.py`.
4. Run `python check.py`.

`_make_golden.py` is for the lead. Do not submit that file.
