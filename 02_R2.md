# R2: ML research, week 1

Member: ____________
Time budget: 8-12 hours across the week.

## What this week gives you

You run every stage of the project yourself with a small model: data, training, evaluation, quantization, and the C runtime. Then you build the claims evidence table: for every published number you find a check you can run now. By the meetup you can explain every stage from memory, with your own numbers.

## Part 1: Run the full pipeline (about 5-6 hours)

Paste the real output of every command into your log. Do not paraphrase.

1. Fork the repo, clone it, and set up the env with `uv sync`.
2. Run the data step: `uv run python -m research.tinystories.prepare --vocab 32768`
   Record how long it takes, the size of the raw folder, and the tokenizer vocab size.
3. Train a small model on Colab: `uv run python -m research.tinystories.train --arm baseline --vocab 32768 --steps 200 --target-core 559000 --seed 0`
   Record the loss at steps 50, 100, 150, and 200. Save the checkpoint to Drive.
4. Sample from your model: `uv run python -m research.tinystories.sample --run runs/<your-run>.pt --tokenizer data/tinystories/vocab-32768/tokenizer.json`
   Save 5 generations and note the quality.
5. Run quantization: `quantize_eval.py` on your run. It will refuse. Record the error message and explain why the tool refuses a single-arm cohort. Read src/quantize.py to find the rule.
6. Build the portable C runtime on host and run its tests. Record the max abs diff.
7. Write PIPELINE_LOG.md with the commands, the outputs, and the errors. Push it to this repo in a PR.

## Part 2: Your depth, claims evidence table (about 4-5 hours)

1. Take these claims from RESULTS.md:
   - tokenizer vocab is 32768
   - golden diff is 1e-5 or below
   - the raw data slice is the first 300 MB of TinyStories
   - baseline ppl is 12.58
   - PLE beats baseline by 0.098 nats
   - model.bin is about 14.9 MB
   - flash row read is 20.3 us
   - on-chip speed is 9.88 tok/s
2. For each claim, run a check that produces evidence now. Use your pipeline outputs, `sha256sum`, `du -sh` on the downloaded files, the source code, and the tracking sheet.
3. For claims you cannot check this week (ppl, on-chip speed), say so and name the week they will be checked.
4. Deliverable: CLAIM_EVIDENCE.md. One row per claim: method, command, result, verdict. Push it to this repo.

## Part 3: Learn with proof (about 2 hours)

1. Watch 3Blue1Brown episodes 1 to 5. Write your own notes, at least 30 lines.
2. Write a numpy MLP that learns XOR, with backprop. Push the code.
3. Write one paragraph: the hardest thing you learned this week and why.

## Part 4: Meetup teach-back

- 10-minute talk with your own numbers: the evidence table, the refusal error, the golden diff.
- The lead asks two questions about your log. You answer from the log, not from memory.

## Evidence checklist (this is what closes your issue)

- [ ] PIPELINE_LOG.md with pasted command outputs
- [ ] At least 3 recorded errors with their fixes
- [ ] CLAIM_EVIDENCE.md with a verdict per claim
- [ ] XOR MLP code that runs
- [ ] Notes and the hardest-thing paragraph
- [ ] Pair sign-off (R1 verifies your log)
- [ ] Meetup talk given

## Pair

Your pair is R1. Sign off each other's logs before the meetup.

## Resources

- Repo: https://github.com/slvDev/esp32-ai
- RESULTS.md: https://github.com/slvDev/esp32-ai/blob/main/RESULTS.md
- 3Blue1Brown neural networks: https://www.youtube.com/playlist?list=PLZHQObOWTQDNU6R1_67000Dx_ZCJB-3pi
- micrograd: https://github.com/karpathy/micrograd
