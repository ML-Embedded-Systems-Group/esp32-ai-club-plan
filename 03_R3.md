# R3 — ML Research, Week 1: the full pipeline in your hands

Member: ____________
Time budget: 8-12 hours across the week.

## What this week gives you

You run every stage of the project yourself with a small model: data, training, evaluation, quantization, and the C runtime. Then you build a tokenizer of your own and compare it with the repo tokenizer. At the end you can explain the whole pipeline from memory, with your own numbers.

## Part 1 — Run the full pipeline (about 5-6 hours)

Do every step. Paste the real output of every command into your log. Do not paraphrase.

1. Fork the repo, clone it, set up the env with `uv sync`.
2. Data: `uv run python -m research.tinystories.prepare --vocab 32768`
   Record: time taken, size of the raw folder, tokenizer vocab size.
3. Train a small model on Colab:
   `uv run python -m research.tinystories.train --arm baseline --vocab 32768 --steps 200 --target-core 559000 --seed 0`
   Record loss at steps 50, 100, 150, 200. Save the checkpoint to Drive.
4. Sample from your model:
   `uv run python -m research.tinystories.sample --run runs/<your-run>.pt --tokenizer data/tinystories/vocab-32768/tokenizer.json`
   Save 5 generations. Note the quality.
5. Quantization: run `quantize_eval.py` on your run. Expect a refusal. Record the error message and explain why the tool refuses a single-arm cohort. Read src/quantize.py to find the rule.
6. Runtime: build the portable C runtime on host. Run its tests. Record the max abs diff.
7. Write PIPELINE_LOG.md: every command, every output, every error, every fix. Push it to this repo in a PR.

## Part 2 — Your depth: a tokenizer of your own (about 4-5 hours)

1. Write a small tokenizer in Python: character level or a simple byte-pair merge. Keep it under 150 lines.
2. Tokenize 100 stories from `data/tinystories/raw/`.
3. Measure: average tokens per story, unique token count, coverage of the repo tokenizer's 32768 tokens by your token set.
4. Compare with the repo tokenizer: load its tokenizer.json, tokenize the same 100 stories, measure the same stats.
5. Answer in writing: why does the repo use 32768 tokens? What breaks with a smaller vocab?
6. Deliverable: your tokenizer code and TOKENIZER_STUDY.md with the comparison table. Push both to this repo.

## Part 3 — Learn with proof (about 2 hours)

1. Watch 3Blue1Brown episodes 1 to 3. Write your own notes, at least 30 lines.
2. Write a numpy MLP that learns XOR, with backprop. Push the code.
3. Write one paragraph: the hardest thing you learned this week and why.

## Part 4 — Meetup teach-back

- 10-minute talk with your own numbers: tokenizer stats, generations, golden diff.
- The lead asks two questions from your PIPELINE_LOG.md. You answer from your log, not from memory.

## Evidence checklist (this is what closes your issue)

- [ ] PIPELINE_LOG.md with pasted command outputs
- [ ] At least 3 recorded errors with their fixes
- [ ] Tokenizer code that runs
- [ ] TOKENIZER_STUDY.md with the comparison table
- [ ] XOR MLP code that runs
- [ ] Notes and the hardest-thing paragraph
- [ ] Pair sign-off (R4 verifies your log)
- [ ] Meetup talk given

## Pair

Your pair is R4. Sign off each other's logs before the meetup.

## Resources

- Repo: https://github.com/slvDev/esp32-ai
- prepare.py: https://github.com/slvDev/esp32-ai/blob/main/research/tinystories/prepare.py
- TinyStories dataset: https://huggingface.co/datasets/roneneldan/TinyStories
- 3Blue1Brown neural networks: https://www.youtube.com/playlist?list=PLZHQObOWTQDNU6R1_67000Dx_ZCJB-3pi
