# H3: ML + embedded, week 1

Member: ____________
Time budget: 6-8 hours across the week.

## Phase 1: Reading (about 2-3 hours)

Read these in order:

1. The model file: https://github.com/slvDev/esp32-ai/blob/main/src/model.bin (use the size and the hex dump, nothing else)
2. The build script: https://github.com/slvDev/esp32-ai/blob/main/src/build.py (read the docstring and the comments, skip the code)
3. The project readme: https://github.com/slvDev/esp32-ai (read the description, skip the code)
4. The results page: https://github.com/slvDev/esp32-ai/blob/main/RESULTS.md (look at the numbers, skip the code)

For each source, write in your log:
- what it is, in one line
- 3 things you learned
- 1 question you still have

Write in your own words. Do not copy-paste.

## Phase 2: Coding, PACK_BIN.py, the packer (about 1-2 hours)

This is the packing step the club runs on real weights. Build it now:

1. Get a raw values file: use RAW.bin from R5 (ask your pair) or generate it: 1000 fp16 values between -1 and 1, struct.pack, 2 bytes per value.
2. Write a script, under 60 lines, stdlib only (struct, math).
3. Read RAW.bin. Split the values into groups of 16. For each group: scale = the largest absolute value. Quantize to 4-bit levels: round to the nearest of 16 levels.
4. Pack: 2 levels per byte, level times 16 plus level. Keep one scale byte per group.
5. Write the packed bytes and the scales to PACKED.bin.
6. Read PACKED.bin back, unpack, dequantize. Verify against the original values. Print the round-trip error.
7. Print: bytes in RAW.bin, bytes in PACKED.bin, the compression ratio.
8. The script must accept any raw values file, so the club can pack real weights in week 2.

Save it as PACK_BIN.py. Run it and keep the output. Write the script yourself. Do not paste from any AI tool.

## Phase 3: Maths, byte accounting (about 1 hour)

Show every step, for the 1000 values:

1. 1000 values times 4 bits. Convert to bytes: divide by 8.
2. Scales: 1000 divided by 16 = 63 groups. 63 scale bytes.
3. Total packed bytes: values bytes plus scale bytes.
4. fp16: 1000 values times 2 bytes.
5. Compression ratio: fp16 bytes divided by packed bytes. Round to 2 decimals.
6. Two sentences: what the scale bytes cost, and how a bigger group changes that.

## Phase 4: Doc generation, PACK_BIN.md (about 1 hour)

Build PACK_BIN.md with these parts:

- The output of PACK_BIN.py.
- The maths from phase 3, every step.

Push PACK_BIN.py, PACK_BIN.md, and PACKED.bin to this repo. Do not push RAW.bin.

## Meetup demo

- 5 minutes: run the packer live and explain the byte math.
- The lead asks you 2 questions about what you made.

## Evidence checklist (this is what closes your issue)

- [ ] Reading log, one entry per source
- [ ] PACK_BIN.py in this repo, with its output
- [ ] PACK_BIN.md in this repo, with output and maths
- [ ] Pair sign-off (H2 checks your log)
- [ ] Meetup demo given

## Pair

Your pair is H2. Check each other's logs before the meetup.

## Resources

- model.bin: https://github.com/slvDev/esp32-ai/blob/main/src/model.bin
- src/build.py: https://github.com/slvDev/esp32-ai/blob/main/src/build.py
- Repo: https://github.com/slvDev/esp32-ai
